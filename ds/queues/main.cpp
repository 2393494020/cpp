#include <iostream>

using namespace std;
/**
 * FILO
 * Input a stack is by push; output is by pop and top.
 * Only the top element is accessible.
 **/
int main(int argc, char const *argv[])
{
    cout << "queues" << endl;
    return 0;
}
